name: Duke Dependency Generator CI

on:
  push:
    branches: [main]
    paths:
      - 'skills/duke-dependency-generator/**'
      - 'packages/duke-dependency-generator-build/**'
  pull_request:
    branches: [main]
    paths:
      - 'skills/duke-dependency-generator/**'
      - 'packages/duke-dependency-generator-build/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/duke-dependency-generator-build
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 10.24.0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: packages/duke-dependency-generator-build/pnpm-lock.yaml
      - run: pnpm install
      - run: pnpm validate
      - run: pnpm build
